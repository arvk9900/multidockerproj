name: Docker CI Pipeline

on:
  push:
    branches:
      - main

jobs:

  build:

    runs-on: self-hosted

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy Containers
        run: |
          cd /multidockerproj
          docker compose up -d --build